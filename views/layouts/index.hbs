<!DOCTYPE html>
<html>
  <head>
      <meta charset="utf-8">
      {{>header}}
      <title>{{title}}</title>
  </head>
  <body>
      {{>nav}}
        <div class="row">
          {{>leftNav}}
          <div class="col-sm-offset-2 col-xs-offset-3 col-md-offset-1 col-sm-10">
            {{{body}}}
          </div>
      </div>
  </body>
</html>
